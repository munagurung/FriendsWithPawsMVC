@model IEnumerable<FriendsWithPawsMVC.Models.Breed>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Specie.SpecieName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BreedName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FoodCostPerKG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HousingCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Specie.SpecieName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BreedName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoodCostPerKG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HousingCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BreedID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BreedID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BreedID })
        </td>
    </tr>
}

</table>
